import {makeStyles} from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    display: 'flex',
    padding: '20px 0px'
  },
  menuItem: {
    paddingRight: 30
  }
}));
